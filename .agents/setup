#!/usr/bin/env bash
set -euo pipefail

step() {
  local label="$1"
  shift
  printf '\n==> %s\n' "$label"
  time "$@"
}

step "Install Node.js dependencies" npm ci
step "Build the site" npm run build
